services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: planner-app
    ports:
      # Visit http://localhost:3003 — nginx serves the static export on port 80.
      - "3003:80"
    restart: unless-stopped
